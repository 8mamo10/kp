#include <bits/stdc++.h>
using namespace std;

int main()
{
  int N, Q;
  cin >> N >> Q;
  vector<int> events(Q);
  for (int i = 0; i < Q; i++)
  {
    int type, player;
    cin >> type >> player;
  }
  return 0;
}