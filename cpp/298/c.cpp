#include <bits/stdc++.h>
using namespace std;

int main()
{
  int N, Q;
  cin >> N >> Q;

  for (int _ = 0; _ < Q; _++)
  {
    int type;
    cin >> type;
    int i, j;
    if (type == 1)
    {
      cin >> i >> j;
    }
    else if (type == 2)
    {
      cin >> i;
    }
    else if (type == 3)
    {
      cin >> i;
    }
  }
  return 0;
}