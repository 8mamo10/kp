#include <bits/stdc++.h>
using namespace std;

// #include <atcoder/modint>
// using namespace atcoder;
// using mint = modint998244353;

using ll = long long;
// using ull = unsigned long long;

int main()
{
  int N, M, D;
  cin >> N >> M >> D;
  vector<ll> A(N);
  for (int i = 0; i < N; i++)
  {
    cin >> A[i];
  }
  vector<ll> B(M);
  for (int i = 0; i < N; i++)
  {
    cin >> B[i];
  }
  return 0;
}